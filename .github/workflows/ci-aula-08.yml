name: "Correcao - Aula 08 (NLP com Big Data)"

on:
  pull_request:
    branches: [master]
    paths:
      - "aula-08-nlp/**"
      - ".github/workflows/ci-aula-08.yml"

permissions:
  contents: read

jobs:
  corrigir-lab:
    name: Rodar testes automaticos do lab
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do codigo da PR
        uses: actions/checkout@v4

      - name: Build da imagem Docker do lab
        working-directory: aula-08-nlp
        run: docker build -t lab-aula-08 .

      - name: Rodar testes (correcao automatica)
        working-directory: aula-08-nlp
        run: |
          set -o pipefail
          docker run --rm lab-aula-08 pytest -v --tb=short | tee test_output.txt

      - name: Publicar resumo da correcao
        if: always()
        shell: bash
        working-directory: aula-08-nlp
        run: |
          {
            echo "## Resultado da correcao automatica - Aula 08 (NLP)"
            echo ""
            echo '```'
            cat test_output.txt 2>/dev/null || echo "Nenhum output de teste disponivel"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
