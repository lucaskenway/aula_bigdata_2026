name: "Correcao - Aula 03 (Kafka e Spark Streaming)"

on:
  pull_request:
    branches: [master]
    paths:
      - "aula-03-kafka-spark-streaming/**"
      - ".github/workflows/ci-aula-03.yml"

permissions:
  contents: read

jobs:
  corrigir-lab:
    name: Rodar testes automaticos do lab
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do codigo da PR
        uses: actions/checkout@v4

      - name: Build da imagem Docker do lab
        working-directory: aula-03-kafka-spark-streaming
        run: docker build -t lab-aula-03 .

      - name: Rodar testes (correcao automatica)
        working-directory: aula-03-kafka-spark-streaming
        run: |
          set -o pipefail
          docker run --rm lab-aula-03 pytest -v --tb=short | tee test_output.txt

      - name: Publicar resumo da correcao
        if: always()
        shell: bash
        working-directory: aula-03-kafka-spark-streaming
        run: |
          {
            echo "## Resultado da correcao automatica - Aula 03 (Kafka / Spark Streaming)"
            echo ""
            echo '```'
            cat test_output.txt 2>/dev/null || echo "Nenhum output de teste disponivel"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
