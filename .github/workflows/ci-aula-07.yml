name: "Correcao - Aula 07 (Machine Learning com Big Data)"

on:
  pull_request:
    branches: [master]
    paths:
      - "aula-07-machine-learning/**"
      - ".github/workflows/ci-aula-07.yml"

permissions:
  contents: read

jobs:
  corrigir-lab:
    name: Rodar testes automaticos do lab
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do codigo da PR
        uses: actions/checkout@v4

      - name: Build da imagem Docker do lab
        working-directory: aula-07-machine-learning
        run: docker build -t lab-aula-07 .

      - name: Rodar testes (correcao automatica)
        working-directory: aula-07-machine-learning
        run: |
          set -o pipefail
          docker run --rm lab-aula-07 pytest -v --tb=short | tee test_output.txt

      - name: Publicar resumo da correcao
        if: always()
        shell: bash
        working-directory: aula-07-machine-learning
        run: |
          {
            echo "## Resultado da correcao automatica - Aula 07 (Machine Learning)"
            echo ""
            echo '```'
            cat test_output.txt 2>/dev/null || echo "Nenhum output de teste disponivel"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
